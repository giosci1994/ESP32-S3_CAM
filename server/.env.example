# Copy this file to `.env` and fill in your environment-specific values.
# Values left blank will fall back to the defaults defined by the application.

# --- Video source -----------------------------------------------------------
# MJPEG stream exposed by the ESP32 camera.
SOURCE_URL=http://esp32-s3.local/stream
# Target frames per second for processing.
TARGET_FPS=25
# Enable verbose logging (1) or keep standard logs (0).
VERBOSE=0

# --- MQTT ------------------------------------------------------------------
MQTT_HOST=mqtt.local
MQTT_PORT=1883
MQTT_USER=mqtt_user
MQTT_PASSWORD=
MQTT_BASE_TOPIC=gesture32
MQTT_DISCOVERY_PREFIX=homeassistant
MQTT_CLIENT_ID=gesture32-server

# --- Pinch detection tuning -------------------------------------------------
PINCH_DEADZONE_PX=8
PINCH_HISTORY=8

# --- Advanced ---------------------------------------------------------------
# JSON object mapping MediaPipe gesture labels to custom values.
CUSTOM_GESTURE_MAP={}
